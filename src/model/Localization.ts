export default interface Localization {
    searchLabel: string,
    searchPlaceholder: string,
    firstPage: string,
    lastPage: string,
    nextPage: string,
    prevPage: string,
    noOption: string,
    statusOf: string
}